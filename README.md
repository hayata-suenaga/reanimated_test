# Reanimated Test

This project is a playground to test features offered by `react-native-reanimated` library.

## Features

- Basic workflow to animate

  [file](./screens/Basic.tsx)

  - `useSharedValue`
  - `useAnimatedStyle(() => (styleObj))`
  - `Animated.View`, `sharedValue.value = withTiming(toValue, configObj)`
